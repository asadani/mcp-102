window.COURSE={
number:102,title:'Build the integration',scope:'Product engineering',protocol:'2026-07-28',readTime:'55 minutes',repo:'https://github.com/asadani/mcp-102',previous:{number:101,url:'https://tech.anujsadani.in/mcp-101/'},next:{number:103,label:'Operate the server',url:'https://tech.anujsadani.in/mcp-103/'},
hero:'Build the <em>integration</em>',lead:'MCP 101 explained the protocol. MCP 102 turns it into a small, believable product: a team task board, a knowledge base, and tools that cross the boundary without losing identity, safety, or meaning.',
chapters:[
{title:'Start with the product boundary',kicker:'Architecture',dek:'The MCP server is an adapter. It should not become a second product backend.',body:'<p>Teamspace has a task service and a knowledge service. Those systems own data, permissions, invariants, and audit history. The MCP layer translates protocol requests into calls to those services.</p><h3>Draw the ownership line</h3><p>The host owns the conversation and model. The MCP server owns tool and resource contracts. The product API owns authorization and business rules. Keeping that line visible prevents a tool handler from becoming an untestable pile of policy.</p>',callout:{label:'Design test',text:'If the web UI and an MCP tool create a task, both paths should reach the same product command and enforce the same rule.'},lab:{kind:'flow',title:'Trace one request',steps:[{name:'Host chooses tool',detail:'The model proposes <code>create_task</code>; the host decides whether and how to call it.'},{name:'Server validates',detail:'The MCP adapter validates shape, resolves identity, and builds a product command.'},{name:'Product authorizes',detail:'Teamspace checks membership and capability against the current tenant.'},{name:'Result returns',detail:'The adapter returns structured content that the model and UI can both inspect.'}]},takeaways:['The protocol adapter does not replace the domain service.','Treat tenant and user identity as request data, never process globals.']},
{title:'Design the first tool',kicker:'Tool contract',dek:'A useful tool is a narrow product capability with an honest schema and a stable outcome.',body:'<p><code>create_task</code> needs a title, team, and an idempotency key. Description and assignee are optional. The description tells the model when to use it; the schema tells the client what can be sent.</p><pre><code>{ name: "create_task", input: { teamId, title, assigneeId? }, idempotencyKey }</code></pre><p>Return the created task as structured content plus a short human-readable summary. Do not encode failure as a sentence that looks successful.</p>',lab:{kind:'choices',title:'Contract pressure test',items:[{name:'Same key, same input',heading:'Replay safely',detail:'Return the original result. A retry must not create a second task.'},{name:'Same key, new input',heading:'Reject the conflict',detail:'Return a deterministic idempotency conflict. Reusing a key for different intent is a caller error.'},{name:'No permission',heading:'Refuse at the product boundary',detail:'The product service denies the operation and records the attempted action.'}]},takeaways:['Tool names describe capabilities; descriptions explain selection conditions.','Mutating tools need explicit replay semantics.']},
{title:'Make failure part of the schema',kicker:'Validation',dek:'Protocol errors, product failures, and empty results are different facts.',body:'<p>A malformed JSON-RPC request is a protocol error. A valid tool call rejected by a business rule is a tool result with an error condition. A search with no matches is a successful empty result.</p><p>This distinction controls retry behavior. Retrying malformed input wastes capacity; retrying a transient dependency failure may work; retrying a permission denial will not.</p>',callout:{label:'Rule',text:'Classify the failure where it originates, preserve a stable machine code, and keep the human message safe to display.'},takeaways:['Validation is layered: protocol envelope, tool input, then product invariant.','An empty collection is data, not failure.']},
{title:'Use resources and prompts on purpose',kicker:'Context primitives',dek:'Tools perform actions. Resources expose reference material. Prompts package a repeatable way to begin.',body:'<p>Expose a team handbook as a resource when the client benefits from reading stable context. Use a prompt such as “prepare weekly triage” to offer a reusable workflow. Neither primitive should quietly mutate data.</p><p>Resource URIs must encode a stable identity and still pass an authorization check when read. Listing a URI is not proof that the caller may fetch it.</p>',takeaways:['Choose the primitive from the user intent, not from what is easiest to code.','Apply tenant scope at list and read time.']},
{title:'Build a deterministic host',kicker:'Reference client',dek:'A scripted host makes the protocol visible before an LLM makes behavior probabilistic.',body:'<p>The repository includes a reference host that connects, initializes, discovers capabilities, and calls tools using a fixed plan. It records every request and response so learners can see the wire.</p><p>Once that path is correct, substitute a model for selection. Keep the same approval gate for writes.</p>',lab:{kind:'flow',title:'Host control loop',steps:[{name:'Initialize',detail:'Negotiate protocol version and capabilities.'},{name:'Discover',detail:'List tools, prompts, and resources only when needed.'},{name:'Plan',detail:'Select a capability using schema and user intent.'},{name:'Approve',detail:'Show consequential writes before execution.'},{name:'Execute',detail:'Send the call and render structured results.'}]},takeaways:['Deterministic fixtures reveal protocol mistakes quickly.','The host retains control of approval and presentation.']},
{title:'Support two transports',kicker:'HTTP and stdio',dek:'Transport changes deployment and framing, not the meaning of a tool.',body:'<p>stdio is ideal for a local child process: one client, process-level trust, and messages over standard streams. Streamable HTTP fits remote multi-user services and standard web infrastructure.</p><p>Keep tool registration and handlers transport-neutral. Only the bootstrap should know whether bytes arrive over stdin or HTTP.</p>',takeaways:['Never log diagnostics to stdout in a stdio server.','Transport authentication belongs at the edge; tool authorization belongs in the product call.']},
{title:'Resolve identity per request',kicker:'Authentication',dek:'A stateless handler must reconstruct the same security context on every call.',body:'<p>Validate the access token, issuer, audience, expiry, and required scopes. Resolve the external subject to a Teamspace actor and tenant. Pass that context explicitly through the adapter.</p><pre><code>request → bearer token → verified claims → actor + tenant → product service</code></pre><p>A process-wide “current user” will eventually leak authority across concurrent requests.</p>',lab:{kind:'choices',title:'Identity failure map',items:[{name:'Wrong audience',heading:'Reject before dispatch',detail:'The token was minted for another resource server.'},{name:'Valid user, wrong team',heading:'Product authorization denies',detail:'Identity is valid; the requested object is outside the actor’s membership.'},{name:'Expired token',heading:'Authenticate again',detail:'Return an authentication challenge. Do not turn expiry into a tool-level permission message.'}]},takeaways:['Authentication proves a principal; authorization permits a specific action.','Tenant scope travels with every product call.']},
{title:'Keep handlers stateless',kicker:'Scale',dek:'Stateless means a request can land on any healthy instance and still produce the same answer.',body:'<p>Store durable state in the product database, idempotency records in shared storage, and resumable task state in a job store. An instance may cache data, but correctness cannot depend on that cache.</p><p>Stateless does not mean memory-free, single-request, or asynchronous. It is a placement property: load balancers do not need sticky routing for correctness.</p>',takeaways:['Externalize correctness-critical state.','Attach correlation and actor context to each request.']},
{title:'Separate missing input from approval',kicker:'Interaction',dek:'“I need a project ID” and “may I publish this?” are different pauses.',body:'<p>Missing input is information required to construct a valid call. Approval is consent for an already understood effect. A client may collect both through UI, but the server must describe which condition blocked progress.</p><p>Do not manufacture defaults for tenant, destination, or destructive scope when the user’s intent is ambiguous.</p>',takeaways:['Request the smallest missing fact.','Keep confirmation in the host where the user can see the effect.']},
{title:'Compose a cross-system workflow',kicker:'Tasks + knowledge',dek:'A release note can join task data and documentation without hiding the systems involved.',body:'<p>The workflow searches completed tasks, reads the release template from the knowledge base, drafts content, and writes only after approval. Each read keeps source attribution. The final write has its own idempotency key.</p>',lab:{kind:'flow',title:'Release-note workflow',steps:[{name:'Search tasks',detail:'Query completed work for the release window and team.'},{name:'Read template',detail:'Fetch the authorized knowledge resource and preserve its version.'},{name:'Draft',detail:'The host model synthesizes a proposed note with task references.'},{name:'Approve',detail:'A person reviews destination, title, and content.'},{name:'Publish',detail:'The product API writes once and returns the canonical URL.'}]},takeaways:['Composition should preserve provenance and authorization at each hop.','Keep the model-generated draft distinct from the committed product record.']},
{title:'Engineer retryable failure',kicker:'Resilience',dek:'Retries are safe only when the operation and the failure classification support them.',body:'<p>Retry transient dependency failures with exponential backoff, jitter, and a strict attempt budget. Do not retry validation, permission, or conflict failures. Carry the same idempotency key across retries of a write.</p><p>Expose a stable code, retryability hint, and correlation ID. Avoid leaking database or token details in the message.</p>',takeaways:['Retry policy belongs to a deadline and a budget.','Idempotency protects effects; it does not make every failure retryable.']},
{title:'Prove the capstone',kicker:'End to end',dek:'A working demo should prove boundaries and failure behavior, not only the happy path.',body:'<p>Run the reference host through discovery, authenticated reads, an approved write, a duplicate replay, and a cross-system workflow. Then test invalid input, wrong-team access, dependency failure, and cancellation.</p><p>The repository labs and tests are the executable companion to this page. Start the product server with <code>npm run dev</code>, the UI with <code>npm run ui</code>, and open <code>/app.html</code>.</p>',callout:{label:'Exit criteria',text:'You can explain who owns each decision, observe every request, and retry a write without duplicating its effect.'},takeaways:['The demo is complete when its failure paths are legible.','MCP 103 begins where correctness meets production pressure.']}
],
quiz:[{q:'Where should team membership be enforced?',options:['Only in the tool description','In the product authorization layer','In the model prompt'],answer:1},{q:'What should a repeated mutating call reuse?',options:['The server process','The idempotency key','A cached access token'],answer:1},{q:'Does stateless MCP imply asynchronous execution?',options:['Yes','Only over HTTP','No'],answer:2},{q:'Who should approve a consequential write?',options:['The host with the user','The downstream database','The tool schema'],answer:0}]
};
